# 5. **Convert to Uppercase**: Convert all strings in a list to uppercase.

listofStr = ['dharmik', 'darshan', 'smit', 'het']

upperCase = list(map(lambda string : string.upper(), listofStr))

print(upperCase)